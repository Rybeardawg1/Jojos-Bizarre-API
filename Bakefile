# Define the default target
default: build

# Define the build target
build:
  # Use your existing Dockerfile in the root directory
  docker build -t "jojos-bizarre-api" .

# Define the push target
push:
  # Push the built image to the Docker registry
  docker push "jojos-bizarre-api"
